#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

#include "../Librerie/ArrayDinamici/array.h"

bool ricerca(vettore_dinamico v, float e, int i) 
{
    assert(i >= 0);
    assert(i < v.dimensione);

    for(int j = i; j < v.dimensione; j++) 
    {
        
    }
}